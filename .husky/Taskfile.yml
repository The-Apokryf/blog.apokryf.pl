
# https://taskfile.dev

version: '3'



vars:
  GREETING: Hello, World!

tasks:
  default:
    cmds:
      - npm i
      - npm i -D
      - task --list-all
    silent: true
  pre-commit:
    cmds:
      - echo "precommit routine"